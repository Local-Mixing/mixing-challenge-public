---
layout: post
title:  "1: Block Cipher"
date:   2026-05-20
---

{% include math.html %}

Our first challenge concerns random reversible circuits. Gowers conjectured that random $$n$$-wire, $$m$$-gate reversible circuits compute pseudorandom functions. If the conjecture holds, then we should be able to use random reversible circuits as a block cipher -- where the key is the description of the circuit $$C$$. Then, $$\mathsf{Enc}(C, x) = C(x)$$ and $$\mathsf{Dec}(C, y)=C^{-1}(y)$$.

This challenge is relatively open-ended: we are just looking for feedback on the construction. Suggestions and attacks -- both concrete and theoretical -- would be appreciate. We do not have a specific challenge to solve here.

## Random Construction

## Balanced Construction

We build a block cipher in layers. In each layer, exactly one gate will be active per wire. Controls are assigned at uniformly at random. Here's a procedure to generate an $m=k\cdot n$ wire balanced block cipher on $$n$$ wires:

> _Repeat $$k$$ times_.
> 1. Initialize an empty set of gates $$G$$.
> 2. For $$i\in[1, n]$$:
>     1. Sample distinct control wires $$c_0,c_1\overset$\gets[1,n]\setminus\{i\}$$.
>     2. Add gate $$(\mathsf{active}=i, (c_0, c_1))$$ to $$G$$.
> 2. Shuffle the elements of $$G$$ and output the layer.

---

Here's an example challenge.

```rust
pub fn we_can() {
  // post code...
  or = algorithms();
}
```

## The challenge

We propose three candidate constructions for a reversible-circuit block cipher:

1. Random reversible circuit. Coupon collector

2. Balanced reversible circuit. Next gate has active pin on the least-popular wire so far.

3. Structured circuit from Claudio/Eduardo old paper